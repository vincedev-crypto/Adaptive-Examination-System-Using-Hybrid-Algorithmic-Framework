spring.application.name=algo

# Database Configuration (MySQL)
spring.datasource.url=jdbc:mysql://localhost:3306/adaptiveexam?createDatabaseIfNotExist=true&useSSL=false&serverTimezone=UTC
spring.datasource.driverClassName=com.mysql.cj.jdbc.Driver
spring.datasource.username=root
spring.datasource.password=

# JPA Configuration
spring.jpa.database-platform=org.hibernate.dialect.MySQLDialect
spring.jpa.hibernate.ddl-auto=update
spring.jpa.show-sql=true
spring.jpa.properties.hibernate.format_sql=true

# Entity and Repository Scanning
spring.jpa.properties.hibernate.packagesToScan=com.exam.entity

# Server Configuration
server.port=8080

# Thymeleaf Configuration
spring.thymeleaf.cache=false

# Email Configuration (Gmail example - IMPORTANT: Configure these before testing!)
# For Gmail: You need to use an App Password, not your regular password
# Generate at: https://myaccount.google.com/apppasswords
spring.mail.host=smtp.gmail.com
spring.mail.port=587
spring.mail.username=vincentjames.contento.mnl@eac.edu.ph
spring.mail.password=lmjf rdth exzd apnv
spring.mail.properties.mail.smtp.auth=true
spring.mail.properties.mail.smtp.starttls.enable=true
spring.mail.properties.mail.smtp.starttls.required=true
spring.mail.properties.mail.smtp.ssl.trust=smtp.gmail.com

# Application Base URL (for email verification links)
app.base.url=http://localhost:8080
